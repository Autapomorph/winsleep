export const DEFAULT_SHOULD_REMEMBER_CONFIGURED_TIME = false;

export const DEFAULT_TIMER_SECONDS = 120;

export const DEFAULT_TIMER_STEP_SECONDS = 30;

export const DEFAULT_NEW_TIMER_PRESET_SECONDS = 1 * 60; // 1m

export const DEFAULT_TIMER_PRESETS = [
  0, // now
  1 * 60, // 1m
  5 * 60, // 5m
  15 * 60, // 15m
  30 * 60, // 30m
  1 * 60 * 60, // 1h
];

export const DEFAULT_IS_CUSTOM_TIMER_STEPS_ENABLED = false;

export const DEFAULT_CUSTOM_TIMER_PRESETS: number[] = [];

export const DEFAULT_IS_LOCKED_BY_DEFAULT = false;

export const SECONDS_IN_DAY = 24 * 60 * 60; // 86400

export const MS_IN_MINUTE = 1000 * 60;
